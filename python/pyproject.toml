[project]
name = "tensorkv"
version = "0.1.0"
description = "TensorKV semantic in-network KV cache"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "TensorKV" }]

[tool.pytest.ini_options]
testpaths = ["python/tests"]
pythonpath = ["python"]
